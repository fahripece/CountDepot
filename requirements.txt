flask>=2.3
flask-compress>=1.14
openpyxl>=3.1
gunicorn>=21.2
bcrypt>=4.0
stripe>=7.0
python-dotenv>=1.0
pdfplumber>=0.10
reportlab>=4.0
sentry-sdk[flask]>=1.40
boto3>=1.34
authlib>=1.3
# SSO/SAML (optional, enterprise only) — NOT in default install.
# Requires system libs first: apt-get install libxmlsec1-dev libxmlsec1-openssl pkg-config
# Then: pip install python3-saml
# The app runs fine without it; the SAML routes return 503 when it's not installed.
